package com.black.core.log;

public interface Log$8$j extends Log9v{

    /***
     * The items printed with strong features have
     * no other special meaning or special performance,
     * but become red font, which can fill in parameters
     * like log4j, but the efficiency is very low
     * @param txt print txt
     * @param params params......
     */
    void fierce(String txt, Object... params);
}
