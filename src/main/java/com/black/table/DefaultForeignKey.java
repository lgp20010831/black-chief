package com.black.table;

public class DefaultForeignKey extends AbstractForeignKey{

    public DefaultForeignKey(ColumnMetadata columnMetadata, PrimaryKey mappingPrimaryKey) {
        super(columnMetadata, mappingPrimaryKey);
    }
}
