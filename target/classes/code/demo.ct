-- 这是注释, ct的全称为 chief template 模板文件
-- 前进两格表示为一个段落, 或者可以用 ##!数字 来表示这个段落
-- 解析的流程首先是, (预处理)段落分离, 参数注入(解析#{}), 段落导入(解析{{xxx}})
  ##!1 这是第一个段落, #{name}, #{A: age>18 ? '成年':'未成年'}
这是第一个段落的第二行
  ##!2 这是第二个段落
{{copy: ##!1}}
--  ##!3 {{A: ##!1 + ##!2}}
